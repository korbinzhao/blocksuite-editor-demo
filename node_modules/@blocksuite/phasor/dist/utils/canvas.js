import { StrokeStyle } from '../consts.js';
export function setLineDash(ctx, strokeStyle) {
    if (strokeStyle === StrokeStyle.Dashed) {
        ctx.setLineDash([12, 12]);
    }
    else {
        ctx.setLineDash([0, 0]);
    }
}
//# sourceMappingURL=canvas.js.map