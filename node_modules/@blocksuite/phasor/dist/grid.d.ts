import { type IBound } from './consts.js';
import type { SurfaceElement } from './elements/surface-element.js';
export declare function compare<T extends {
    id: string;
    index: string;
}>(a: T, b: T): number;
export declare class GridManager {
    private _grids;
    private _elementToGrids;
    private _createGrid;
    private _getGrid;
    get isEmpty(): boolean;
    add(element: SurfaceElement): void;
    remove(element: SurfaceElement): void;
    boundHasChanged(a: IBound, b: IBound): boolean;
    search(bound: IBound): SurfaceElement[];
    pick(x: number, y: number): SurfaceElement[];
}
//# sourceMappingURL=grid.d.ts.map